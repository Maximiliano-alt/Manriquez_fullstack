import { FilterListClientesPipe } from './filter-list-clientes.pipe';

describe('FilterListClientesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListClientesPipe();
    expect(pipe).toBeTruthy();
  });
});
